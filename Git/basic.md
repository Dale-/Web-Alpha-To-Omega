## create a new repository
 ```bash
  git init
 ```

## add & commit
```bash
   git add <filename>
   git commit -m "Commit message"
```

## pushing changes
```bash
  git push origin master
```

## branching
